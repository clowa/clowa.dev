#!/command/with-contenv sh
# Dedicated log sink for the otel-collector: writes to /var/log/otel-collector/current,
# rotates at ~1 MiB, keeps 20 archives, and runs as `nobody`.
exec s6-setuidgid nobody s6-log -b n20 s1000000 /var/log/otel-collector
