#!/command/with-contenv sh
# Dedicated log sink for the api: writes to /var/log/api/current, rotates at
# ~1 MiB, keeps 20 archives, and runs as `nobody`.
exec s6-setuidgid nobody s6-log -b n20 s1000000 /var/log/api
