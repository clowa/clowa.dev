# 301 qunis.day -> qunisday.qunis.de (path + query preserved via {uri}).
#
# NOTE: for this to reach Caddy in production, `qunis.day` must also be attached
# as a custom_domain in serverless.yml (and its DNS pointed at Scaleway).
qunis.day:80 {
	redir https://qunisday.qunis.de{uri} permanent
}
